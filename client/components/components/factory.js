'use strict';

/**
 * A wrapper around the ComponentFactory object, defined on the window.
 * This factory code is shared with the server.
 */

angular
	.module('siteApp')
	.factory('ComponentFactory', [function () {

		return ComponentFactory;
	}]);
