div#navbar.row.transparent.green(ng-include='"components/navbar/navbar.html"')

#signup.page.underlay-navbar

form(name="form", ng-submit="register(form)", novalidate="").ui-modal
  .ui-modal-header
    h2 Higherframe is in closed beta
    
  .ui-modal-body
    div.ui-control.block
      p.b1 Thanks for your interest in Higherframe. We want to create the best wireframing tool we can; a piece of software we hope you'll love.
      p.b1 If you would like to become a beta tester, please provide your details. We appreciate your help and will contact you soon.
      
    div.ui-control.block
      label.ui-label.b1(for="name") Name:
      input.ui-input(name="name", type="text", ng-model="user.name", placeholder="e.g.: Don Draper", required)
      p.ui-error(ng-show='form.name.$error.required && submitted')
        | A name is required
        
    div.ui-control.block
      label.ui-label.b1(for="email") Email address:
      input.ui-input(name="email", type="email", ng-model="user.email", placeholder="e.g.: don@sterlingcooper.com", required, mongoose-error="")  
      p.ui-error(ng-show='form.email.$error.email && submitted')
        | Doesn't look like a valid email.
      p.ui-error(ng-show='form.email.$error.required && submitted')
        | What's your email address?
      p.ui-error(ng-show='form.email.$error.mongoose')
        | {{ errors.email }}
          
    div.ui-control.block
      label.ui-label.b1(for="password") Password:
      input.ui-input(name="password", type="password", ng-model="user.password", required, mongoose-error="")
      p.ui-error(ng-show='(form.password.$error.minlength || form.password.$error.required) && submitted')
        | Password must be at least 3 characters.
      p.ui-error(ng-show='form.password.$error.mongoose')
        | {{ errors.password }}
    
  .ui-modal-footer
    .ui-modal-buttons
      a.ui-button.neutral(ng-click="onCancelButtonClick()")
        span.b1.medium.loose.uppercase Cancel
        
      button.ui-button.success(type="submit")
        span.b1.medium.loose.uppercase Request access
